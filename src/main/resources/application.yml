spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/autopecas
    username: bruno
    password: bruno101

  spring:
    jackson:
      serialization:
        FAIL_ON_EMPTY_BEANS=false
